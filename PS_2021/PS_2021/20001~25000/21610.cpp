#include <bits/stdc++.h>
#define MAX 51
using namespace std;

int N, M;
int board[MAX][MAX];

int main() {
  scanf("%d %d", &N, &M);
  for (int i = 0; i < N; i++) {
    for (int j = 0; j < N; j++) {
      scanf("%d", &board[i][j]);
    }
  }
  for (int i = 0; i < N; i++) {
  }
}