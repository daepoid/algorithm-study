#include <bits/stdc++.h>
#define MAX 101
using namespace std;

int N, M;
int answer = 2100000000;
int cost[MAX], mm[MAX];
int dp[MAX][MAX];

int main() {
  scanf("%d %d", &N, &M);
  for (int i = 1; i < N + 1; i++) {
    scanf("%d", &mm[i]);
  }
  for (int i = 1; i < N + 1; i++) {
    scanf("%d", &cost[i]);
  }

  for (int i = 1; i < N + 1; i++) {
    for (int j = 1; j < MAX; j++) {
      if ()
    }
  }

  printf("%d\n", answer);
}